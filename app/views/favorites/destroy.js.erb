document.getElementById("favorite-<%= @community.id %>").remove();
if(document.getElementById("<%= @community.id %>")) {
  document.getElementById("<%= @community.id %>").innerHTML = `
  <%= render partial: "favorite", locals: { community: @community } %>`;
}